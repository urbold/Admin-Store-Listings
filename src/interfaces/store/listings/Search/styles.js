export default theme => ({
  root: {
    display: "flex",
    alignItems: "center",
    marginTop: "0.5em"
  },
  input: {
    padding: theme.spacing(2)
  },
  label: {
    transform: "translate(14px, 10px) scale(1)"
  }
});
